---
phase: 12-server-side-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/grid/GridView.tsx
autonomous: true

must_haves:
  truths:
    - "Typing in the search bar triggers a server-side query — only matching rows are fetched"
    - "Rows not matching the search term are absent from the grid"
    - "The row count updates to reflect the filtered result set"
    - "Clearing the search restores the full unfiltered row set"
    - "Search works correctly combined with active filter and sort rules"
  artifacts:
    - path: "src/components/grid/GridView.tsx"
      provides: "searchQuery wired to all server queries and cache keys"
      contains: "searchQuery"
  key_links:
    - from: "fetchPage callback"
      to: "utils.row.getByOffset.fetch"
      via: "searchQuery parameter"
      pattern: "getByOffset\\.fetch\\(\\{[^}]*searchQuery"
    - from: "row.count useQuery"
      to: "api.row.count"
      via: "searchQuery in query input"
      pattern: "row\\.count\\.useQuery\\(\\{[^}]*searchQuery"
    - from: "cache-reset useEffect"
      to: "resetCache + refetchCount"
      via: "searchQuery in dep array"
      pattern: "\\[filters, sorts, searchQuery\\]"
    - from: "createRow.onMutate"
      to: "utils.row.count.getData/setData"
      via: "searchQuery in cache key"
      pattern: "row\\.count\\.(getData|setData)\\(\\{[^}]*searchQuery"
---

<objective>
Wire searchQuery to all server-side queries in GridView.tsx so search filters rows at the database level instead of only highlighting client-side matches.

Purpose: The server-side row.getByOffset and row.count procedures already accept and apply searchQuery via ILIKE filtering. The only gap is that GridView.tsx never passes searchQuery to these calls. This plan closes that gap with six targeted edits in a single file.

Output: GridView.tsx updated — search triggers server-side filtering, row count reflects matches, cache resets on search change, and optimistic mutations use the correct cache keys.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-server-side-search/12-RESEARCH.md
@src/components/grid/GridView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire searchQuery to server queries and cache reset</name>
  <files>src/components/grid/GridView.tsx</files>
  <action>
Make the following targeted edits to GridView.tsx. Each edit is small and surgical — do not restructure surrounding code.

**Edit 1 — row.count query (line 95):**
Change `{ tableId, filters }` to `{ tableId, filters, searchQuery }`.
Before: `{ tableId, filters },`
After:  `{ tableId, filters, searchQuery },`

**Edit 2 — fetchPage getByOffset.fetch call (lines 286-292):**
Add `searchQuery` to the fetch input object.
Before:
```
const data = await utils.row.getByOffset.fetch({
  tableId,
  offset: pageIndex * PAGE_SIZE,
  limit: PAGE_SIZE,
  filters,
  sorts,
});
```
After:
```
const data = await utils.row.getByOffset.fetch({
  tableId,
  offset: pageIndex * PAGE_SIZE,
  limit: PAGE_SIZE,
  filters,
  sorts,
  searchQuery,
});
```

**Edit 3 — fetchPage useCallback dep array (line 306):**
Add `searchQuery` to the dependency array.
Before: `[tableId, utils.row.getByOffset, filters, sorts]`
After:  `[tableId, utils.row.getByOffset, filters, sorts, searchQuery]`

**Edit 4 — cache-reset useEffect dep array (line 329):**
Add `searchQuery` to the dependency array. Keep the eslint-disable-line comment.
Before: `}, [filters, sorts]); // eslint-disable-line react-hooks/exhaustive-deps`
After:  `}, [filters, sorts, searchQuery]); // eslint-disable-line react-hooks/exhaustive-deps`

**Important:** Do NOT remove the `// eslint-disable-line` comment. It suppresses warnings about `resetCache` and `refetchCount` not being in the dep array (intentional — they are stable refs).
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. The searchQuery state is already `string`, which matches the expected input type for row.count and row.getByOffset.
  </verify>
  <done>
fetchPage passes searchQuery to getByOffset; row.count query includes searchQuery; cache resets when searchQuery changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix optimistic mutation cache keys and handleBulkCreate</name>
  <files>src/components/grid/GridView.tsx</files>
  <action>
Continue editing GridView.tsx with these remaining changes:

**Edit 5 — createRow.onMutate count cache key (line 585):**
Add `searchQuery` to the getData call.
Before: `const currentCount = utils.row.count.getData({ tableId: mutTableId, filters })?.count ?? totalCount;`
After:  `const currentCount = utils.row.count.getData({ tableId: mutTableId, filters, searchQuery })?.count ?? totalCount;`

**Edit 6 — createRow.onMutate setData call (line 595):**
Add `searchQuery` to the setData call.
Before: `utils.row.count.setData({ tableId: mutTableId, filters }, (old) => ({`
After:  `utils.row.count.setData({ tableId: mutTableId, filters, searchQuery }, (old) => ({`

**Edit 7 — createRow.onError setData call (line 628):**
Add `searchQuery` to the setData call.
Before: `utils.row.count.setData({ tableId: mutTableId, filters }, (old) => ({`
After:  `utils.row.count.setData({ tableId: mutTableId, filters, searchQuery }, (old) => ({`

**Edit 8 — handleBulkCreate getByOffset.fetch calls (lines 548 and 571):**
Both `.fetch({ tableId, offset: 0, limit: PAGE_SIZE, filters, sorts }, ...)` calls need `searchQuery`.
Before (both occurrences):
```
.fetch({ tableId, offset: 0, limit: PAGE_SIZE, filters, sorts }, { staleTime: 0 })
```
After (both occurrences):
```
.fetch({ tableId, offset: 0, limit: PAGE_SIZE, filters, sorts, searchQuery }, { staleTime: 0 })
```

**Edit 9 — handleBulkCreate useCallback dep array (line 580):**
Add `searchQuery` to the dependency array. Keep the eslint-disable-line comment.
Before: `}, [tableId, bulkCreate, refetchCount, utils.row.getByOffset, filters, sorts]); // eslint-disable-line react-hooks/exhaustive-deps`
After:  `}, [tableId, bulkCreate, refetchCount, utils.row.getByOffset, filters, sorts, searchQuery]); // eslint-disable-line react-hooks/exhaustive-deps`
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Run `npx next build` — build succeeds with no errors.
  </verify>
  <done>
All optimistic mutation cache keys include searchQuery. handleBulkCreate fetches with searchQuery. No stale cache key mismatches remain.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full behavior chain:

1. **Type check**: `npx tsc --noEmit` passes
2. **Build**: `npx next build` succeeds
3. **Grep confirmation** — all six query/cache touchpoints include searchQuery:
   - `grep -n "searchQuery" src/components/grid/GridView.tsx` shows searchQuery in:
     - row.count.useQuery input
     - getByOffset.fetch input (fetchPage)
     - getByOffset.fetch input (handleBulkCreate, 2 occurrences)
     - fetchPage dep array
     - cache-reset effect dep array
     - handleBulkCreate dep array
     - createRow.onMutate getData
     - createRow.onMutate setData
     - createRow.onError setData
4. **No removed code**: Client-side highlight system (searchMatches, highlightText, prev/next) remains intact
</verification>

<success_criteria>
- searchQuery is passed to every getByOffset.fetch call in the file
- searchQuery is passed to the row.count.useQuery input
- searchQuery is in the cache-reset useEffect dep array
- All optimistic count getData/setData calls use matching cache keys with searchQuery
- Client-side highlight (searchMatches, prev/next navigation) still present and functional
- TypeScript compiles without errors
- Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-server-side-search/12-01-SUMMARY.md`
</output>
