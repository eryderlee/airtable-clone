---
phase: 03-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(app)/page.tsx
  - src/app/(app)/base/[baseId]/layout.tsx
  - src/app/(app)/base/[baseId]/page.tsx
  - src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/layout.tsx
  - src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/page.tsx
  - src/components/ui/InlineEdit.tsx
autonomous: true

must_haves:
  truths:
    - "Authenticated users are redirected to their first base from /"
    - "Unauthenticated users are redirected to /sign-in"
    - "URL structure is /base/[baseId]/[tableId]/view/[viewId] with no (app) in the URL"
    - "Layout shells nest correctly: sidebar wraps tab bar wraps views panel wraps content"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "Auth guard + sidebar shell"
      contains: "auth()"
    - path: "src/app/(app)/page.tsx"
      provides: "Redirect to first base"
      contains: "redirect"
    - path: "src/app/(app)/base/[baseId]/layout.tsx"
      provides: "Tab bar shell"
      contains: "await params"
    - path: "src/app/(app)/base/[baseId]/page.tsx"
      provides: "Redirect to first table+view"
      contains: "redirect"
    - path: "src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/layout.tsx"
      provides: "Views panel shell"
      contains: "await params"
    - path: "src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/page.tsx"
      provides: "Content area placeholder"
    - path: "src/components/ui/InlineEdit.tsx"
      provides: "Reusable click-to-edit text component"
      contains: "onDoubleClick"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "~/server/auth"
      via: "auth() session check"
      pattern: "auth\\(\\)"
    - from: "src/app/(app)/page.tsx"
      to: "~/trpc/server"
      via: "server-side tRPC caller for base.getAll"
      pattern: "api\\.base\\.getAll"
    - from: "src/app/(app)/base/[baseId]/page.tsx"
      to: "~/trpc/server"
      via: "server-side tRPC caller for table.getByBaseId + view.getByTableId"
      pattern: "table\\.getByBaseId|view\\.getByTableId"
---

<objective>
Create the Next.js App Router nested layout hierarchy for the navigation shell and a shared InlineEdit component.

Purpose: Establishes the route structure that all Phase 3 UI components slot into. The (app) route group scopes the authenticated layout without affecting URLs. Server-side redirects ensure users always land on a valid base/table/view URL.

Output: 6 route files (3 layouts + 2 redirect pages + 1 placeholder page) and 1 shared component.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-shell/03-RESEARCH.md

@src/app/layout.tsx
@src/trpc/server.ts
@src/server/api/trpc.ts
@src/server/api/routers/base.ts
@src/server/api/routers/table.ts
@src/server/api/routers/view.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (app) route group with nested layouts and redirect pages</name>
  <files>
    src/app/(app)/layout.tsx
    src/app/(app)/page.tsx
    src/app/(app)/base/[baseId]/layout.tsx
    src/app/(app)/base/[baseId]/page.tsx
    src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/layout.tsx
    src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/page.tsx
  </files>
  <action>
    Create the full route hierarchy. Critical patterns to follow:

    **1. `(app)/layout.tsx` — Server component, auth guard:**
    - Import `auth` from `~/server/auth` (NOT ~/server/auth/index — the project uses `~/server/auth`)
    - Call `const session = await auth()` — if no session, `redirect("/sign-in")`
    - Render a flex row container (`flex h-screen w-screen overflow-hidden`) with:
      - A placeholder `<aside>` for the sidebar (Plan 02 will replace with AppSidebar client component). For now, render `{children}` only — the sidebar component is created in Plan 02. Instead, accept `children` and render the outer flex container. The sidebar itself is a client component passed via composition.
      - Actually: render the full shell structure. The sidebar is a client component imported here. Since it doesn't exist yet, create a minimal placeholder `AppSidebar` component inline or import from a stub. Better approach: just render the layout structure with a placeholder div for the sidebar area, and `{children}` for the main content. Plan 02 will create the real components and this layout will be updated to import them.
    - SIMPLEST APPROACH: Render `<div className="flex h-screen w-screen overflow-hidden"><aside className="flex w-64 flex-shrink-0 flex-col border-r border-gray-200 bg-white p-3"><p className="text-sm text-gray-400">Bases loading...</p></aside><div className="flex flex-1 flex-col overflow-hidden">{children}</div></div>`. Plan 02 replaces the aside with the real AppSidebar.

    **2. `(app)/page.tsx` — Server component, redirect to first base:**
    - Import `api` from `~/trpc/server` (the server-side RSC tRPC caller)
    - Import `redirect` from `next/navigation`
    - Import `auth` from `~/server/auth`
    - Call `const session = await auth()` — redirect to `/sign-in` if no session
    - Call `const bases = await api.base.getAll()` — this uses the prefetch/caller pattern from `~/trpc/server`
    - If bases is empty, render an empty state with a message "No bases yet" and instructions
    - If bases has items, `redirect(`/base/${bases[0]!.id}`)` — redirect OUTSIDE try/catch (redirect throws internally)

    **3. `base/[baseId]/layout.tsx` — Server component, tab bar shell:**
    - CRITICAL: `params` is `Promise<{ baseId: string }>` — MUST `await params` (Next.js 15)
    - `const { baseId } = await params;`
    - Render a flex column with a placeholder header for the tab bar (`<header className="flex h-10 flex-shrink-0 items-center border-b border-gray-200 bg-white px-2"><span className="text-sm text-gray-400">Tables loading...</span></header>`) and `{children}` below
    - Plan 02 replaces the header with the real TableTabBar client component

    **4. `base/[baseId]/page.tsx` — Server component, redirect to first table+view:**
    - `params` is `Promise<{ baseId: string }>` — MUST await
    - Use server-side tRPC: `const tables = await api.table.getByBaseId({ baseId })`
    - If no tables, render empty state "No tables yet"
    - If tables exist, get views: `const views = await api.view.getByTableId({ tableId: tables[0]!.id })`
    - Redirect: `redirect(`/base/${baseId}/${tables[0]!.id}/view/${views[0]!.id}`)`
    - IMPORTANT: Call redirect OUTSIDE try/catch

    **5. `[tableId]/view/[viewId]/layout.tsx` — Server component, views panel shell:**
    - `params` is `Promise<{ baseId: string; tableId: string; viewId: string }>` — MUST await
    - Render a flex row with a placeholder aside for views panel (`<aside className="flex w-48 flex-shrink-0 flex-col border-r border-gray-200 bg-gray-50 p-2"><span className="text-sm text-gray-400">Views loading...</span></aside>`) and `<main className="flex flex-1 overflow-hidden">{children}</main>`
    - Plan 02 replaces the aside with the real ViewsPanel client component

    **6. `[tableId]/view/[viewId]/page.tsx` — Content area placeholder:**
    - `params` is `Promise<{ baseId: string; tableId: string; viewId: string }>` — MUST await
    - Render a centered placeholder: "Grid will be rendered here in Phase 4" with the current tableId and viewId displayed for verification
    - This page is replaced by the real grid in Phase 4

    **Auth import:** `import { auth } from "~/server/auth"` — confirmed from existing codebase grep.
    **tRPC server import:** `import { api } from "~/trpc/server"` — confirmed from src/trpc/server.ts which exports `{ trpc: api, HydrateClient }`.
  </action>
  <verify>
    Run `npm run build` — must pass with zero errors. Verify no `sync-dynamic-apis` warnings (all params are awaited). Navigate to `http://localhost:3000` when signed in — should redirect through the chain to a base/table/view URL (if data exists) or show empty state.
  </verify>
  <done>
    All 6 route files exist. Build passes. Authenticated user hitting `/` is redirected to `/base/[baseId]/[tableId]/view/[viewId]`. Unauthenticated user is redirected to `/sign-in`. Layout nesting renders sidebar placeholder > tab bar placeholder > views panel placeholder > content placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create InlineEdit shared component</name>
  <files>
    src/components/ui/InlineEdit.tsx
  </files>
  <action>
    Create a reusable click-to-edit text component used by sidebar (base rename), tab bar (table rename), and views panel (view rename) in Plan 02.

    **Component: `InlineEdit`**
    - `"use client"` directive
    - Props: `{ value: string; onSave: (newValue: string) => void; className?: string }`
    - State: `editing` (boolean), `draft` (string)
    - Ref: `inputRef` for auto-focus/select
    - Display mode: `<span>` with `onDoubleClick` to enter edit mode. Render the `value` text.
    - Edit mode: `<input>` with:
      - Auto-focus and select-all on mount via `useEffect` checking `editing`
      - `onBlur` → save (trim, check non-empty and changed, then call `onSave`)
      - `onKeyDown` Enter → save, Escape → revert draft to original value and exit edit mode
      - If trimmed draft is empty or unchanged, revert without calling onSave
    - Apply `className` prop to both span and input for consistent sizing
    - Input should have additional classes: `outline-none bg-transparent` to blend into the container
  </action>
  <verify>
    TypeScript compiles: `npx tsc --noEmit` passes. Component file exists at `src/components/ui/InlineEdit.tsx` with proper "use client" directive, exports `InlineEdit`.
  </verify>
  <done>
    InlineEdit component is exported and ready for use in Plan 02's sidebar, tab bar, and views panel components. Double-click enters edit mode, blur/Enter saves, Escape cancels.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- No `sync-dynamic-apis` warnings in build output
- All params destructured via `await params` (not synchronous access)
- Route files exist at correct paths under `src/app/(app)/`
- InlineEdit component exists at `src/components/ui/InlineEdit.tsx`
</verification>

<success_criteria>
- Build passes cleanly
- Route hierarchy creates the URL pattern `/base/[baseId]/[tableId]/view/[viewId]`
- Auth guard in (app)/layout.tsx redirects unauthenticated users
- Server-side redirects work for index pages (/ → first base, /base/[id] → first table+view)
- Placeholder layouts render in correct nesting order
- InlineEdit component compiles and exports correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-shell/03-01-SUMMARY.md`
</output>
