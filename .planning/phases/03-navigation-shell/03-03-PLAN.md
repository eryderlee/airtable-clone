---
phase: 03-navigation-shell
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/nav/AppSidebar.tsx
  - src/components/nav/TableTabBar.tsx
  - src/components/nav/ViewsPanel.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Sidebar width matches Airtable (~260px)"
    - "Tab bar height matches Airtable (~40px)"
    - "Views panel width matches Airtable (~200px)"
    - "Colors, typography, and spacing are Airtable-accurate"
    - "Content area fills remaining space"
  artifacts:
    - path: "src/components/nav/AppSidebar.tsx"
      provides: "Pixel-accurate sidebar styling"
    - path: "src/components/nav/TableTabBar.tsx"
      provides: "Pixel-accurate tab bar styling"
    - path: "src/components/nav/ViewsPanel.tsx"
      provides: "Pixel-accurate views panel styling"
  key_links: []
---

<objective>
Match the navigation shell layout to Airtable's exact visual design — spacing, colors, typography, dimensions — using Playwright MCP to capture reference screenshots.

Purpose: The standing UI Fidelity Checkpoint in the roadmap requires pixel-accurate matching against Airtable. This plan does the visual polish pass after all functionality is in place.

Output: Updated styling across all shell components matching Airtable's visual design.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-shell/03-RESEARCH.md
@.planning/phases/03-navigation-shell/03-02-SUMMARY.md

@src/components/nav/AppSidebar.tsx
@src/components/nav/TableTabBar.tsx
@src/components/nav/ViewsPanel.tsx
@src/app/(app)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Airtable reference layout via Playwright and apply pixel-accurate styling</name>
  <files>
    src/components/nav/AppSidebar.tsx
    src/components/nav/TableTabBar.tsx
    src/components/nav/ViewsPanel.tsx
    src/app/(app)/layout.tsx
    src/app/(app)/base/[baseId]/[tableId]/view/[viewId]/page.tsx
  </files>
  <action>
    **Step 1: Capture Airtable reference using Playwright MCP**
    - Use Playwright MCP to navigate to airtable.com and capture screenshots of the layout structure
    - Focus on: sidebar (base list area), tab bar (table tabs), views panel (left panel within a base), and the overall page structure
    - Note exact measurements: sidebar width, tab bar height, views panel width, font sizes, colors, border styles, background colors, padding, gaps
    - If Airtable requires login and Playwright can't access it, use the research file's reference dimensions as fallback:
      - Sidebar: ~260px wide, dark background (#1b1e3d or similar dark blue/purple), white text
      - Tab bar: ~40px height, white background, gray borders, colored active tab indicator
      - Views panel: ~200px wide, light gray background, compact text
      - Toolbar area: ~45px (not built yet, but reserve space awareness)

    **Step 2: Apply Airtable-accurate styling to all shell components**

    Based on the Playwright scrape (or research fallback), update Tailwind classes across all components:

    **AppSidebar.tsx:**
    - Airtable's sidebar has a dark background (navy/dark purple), white text, compact items
    - Update width, background color, text color, hover states, active item highlight
    - Font sizes should be small (text-sm or text-xs)
    - Base items should have hover background, active background distinct color
    - "+" button styling, padding, border radius

    **TableTabBar.tsx:**
    - Airtable tabs: white background, bottom border, active tab has a colored bottom border (blue/teal)
    - Tab items: compact padding, text-sm, hover state
    - "+" button styled as a subtle icon
    - Remove any existing styling that doesn't match Airtable

    **ViewsPanel.tsx:**
    - Light background, compact list of view names
    - Active view has subtle highlight
    - Small font, minimal padding

    **Layout containers:**
    - Ensure the flex layout dimensions match: sidebar fixed width, tab bar fixed height, views panel fixed width
    - Content area fills all remaining space
    - No scroll on the outer container (overflow-hidden on the shell, scroll only in content area)

    **Content placeholder page:**
    - Update the placeholder to look appropriate within the shell (light background, centered text)
  </action>
  <verify>
    `npm run build` passes. Run dev server and visually compare the shell layout against Airtable. Sidebar, tab bar, and views panel should closely match Airtable's dimensions, colors, and typography.
  </verify>
  <done>
    Shell layout visually matches Airtable's design: dark sidebar with correct width, white tab bar with colored active indicator, light views panel, content area fills remaining space. All dimensions and colors are Airtable-accurate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete navigation shell with Airtable-accurate styling:
    - Left sidebar (dark, ~260px) showing bases with create/rename/delete
    - Top tab bar (~40px) showing table tabs with create/rename/delete
    - Views panel (~200px) listing views with active highlight
    - Content area placeholder filling remaining space
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 — should redirect to a base/table/view URL
    2. Compare sidebar appearance against Airtable (open airtable.com in another tab)
    3. Compare tab bar appearance against Airtable
    4. Compare views panel appearance against Airtable
    5. Verify CRUD operations still work after styling changes:
       - Create a new base (appears in sidebar)
       - Double-click base name to rename
       - Create a new table (appears as tab, has seeded data)
       - Double-click tab to rename
       - Switch between tables by clicking tabs
       - Switch between views in the views panel
    6. Verify no visual artifacts: overflow, clipping, misalignment
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Layout dimensions match Airtable reference (sidebar ~260px, tab bar ~40px, views panel ~200px)
- Color scheme matches Airtable (dark sidebar, white tab bar, light views panel)
- Typography matches (font sizes, weights)
- All CRUD functionality still works after styling updates
- No overflow or layout breakage
- User approves visual match
</verification>

<success_criteria>
- Shell layout is visually indistinguishable from Airtable at a glance
- All Phase 3 success criteria from the roadmap are met:
  1. User can create, rename, delete a base from the sidebar
  2. User can create, rename, delete a table from the tab bar
  3. New tables are pre-populated with faker.js data
  4. User can switch tables by clicking tabs (URL updates, no full reload)
  5. Layout matches Airtable 1:1: sidebar, tab bar, views panel, content area
- User has manually verified the result (standing UI Fidelity Checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-shell/03-03-SUMMARY.md`
</output>
